---
'issueforge': patch
---

Explain an unreachable repository in one sentence instead of echoing five lines of
git output, and point new users at the local `run` before the self-hosted runner
setup.
